// TODO: this should be better

var should = require('should');

describe('Log Lite', function () {

  it('should be requireable', function (done) {
    var LogLite = require('../');
    done();
  });

});
